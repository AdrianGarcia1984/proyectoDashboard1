import React from 'react';
import { Layout } from './Layout';

export const Clientes = () => {
    return (
        <Layout>
            <div>
                <h1>Clientes</h1>
            </div>
        </Layout>

    );
};